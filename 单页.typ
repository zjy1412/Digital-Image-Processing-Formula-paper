#set page(
  width: 21cm,
  height: 29.7cm,
  margin: (top:0.5cm, bottom:0.5cm, left:0.5cm, right:0.5cm),
  // columns: 2
)

// #set page(
  // width: 29.7cm,
  // height: 21cm,
//   margin: (top:0.5cm, bottom:0.5cm, left:0.5cm, right:0.5cm),
//   columns: 4, // 设置为4栏
// )
// #set par(spacing: 1.5em) // 设置段落之间的间距

#include("bg.typ")
#include("ch2.typ")
#include("ch3.typ")
#include("ch4.typ")
#include("ch5.typ")